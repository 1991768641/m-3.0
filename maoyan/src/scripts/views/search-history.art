{{each historylist}}
<div class="history-item">
    <span class="history-icon"></span>
    <span class="word one-line">{{$value}}</span>
    <div class="del-word" data-index="{{$index}}"></div>
</div>
{{/each}}